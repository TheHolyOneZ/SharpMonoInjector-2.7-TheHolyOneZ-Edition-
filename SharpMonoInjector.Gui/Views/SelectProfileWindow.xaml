<Window x:Class="SharpMonoInjector.Gui.Views.SelectProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Profile Name" Height="500" Width="600"
        WindowStyle="None" ResizeMode="CanResize" Background="#FF1A1A1A" AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">
    <Border BorderBrush="#FF00E676" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- Modern Title Bar -->
            <Border Grid.Row="0" Background="#FF000000" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="20,0,0,0">
                        <TextBlock Text="✏️" FontSize="18" VerticalAlignment="Center" Foreground="#FF00E676" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Title}" Foreground="#FF00E676" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Grid.Column="2" Content="✕" Width="40" Height="40" Background="Transparent" Foreground="#FFFFFFFF"
                            BorderThickness="0" FontSize="16" Click="Close_Click" Cursor="Hand" Margin="0,0,10,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFFF4444"/>
                                        <Setter Property="Foreground" Value="#FFFFFFFF"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Profile Name Input Section -->
                <Border Grid.Row="0" Background="#FF252525" BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="🎯 Profile Name" FontSize="14" FontWeight="SemiBold" Foreground="#FF00E676" Margin="0,0,0,10"/>
                        <TextBox Grid.Row="1" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBox}" FontSize="16" Padding="12,10"
                                 ToolTip="Enter a descriptive name for this injection profile"/>
                    </Grid>
                </Border>

                <!-- Profile Options -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="⚙️ Profile Configuration" FontSize="14" FontWeight="SemiBold" Foreground="#FF00E676" Margin="0,0,0,15"/>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,0,10,0">
                            <!-- Use Current Settings Option -->
                            <Border Background="#FF1E1E1E" BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <RadioButton Grid.Column="0" IsChecked="{Binding UseCurrentSettings}" Style="{StaticResource ModernRadioButton}" VerticalAlignment="Top" Margin="0,0,15,0"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="Use Current Settings" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFFFF" Margin="0,0,0,5"/>
                                        <TextBlock Text="Apply the current injection settings without saving them to a profile" FontSize="12" Foreground="#FF888888" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Use Saved Profile Option -->
                            <Border Background="#FF1E1E1E" BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <RadioButton Grid.Column="0" x:Name="rbUseProfile" IsChecked="{Binding UseCurrentSettings, Converter={StaticResource InverseBooleanConverter}}"
                                                     Style="{StaticResource ModernRadioButton}" VerticalAlignment="Top" Margin="0,0,15,0"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="Use Saved Profile" FontSize="14" FontWeight="SemiBold" Foreground="#FFFFFFFF" Margin="0,0,0,5"/>
                                            <TextBlock Text="Select from your saved injection profiles" FontSize="12" Foreground="#FF888888" TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Profile List -->
                                    <Border Grid.Row="1" Background="#FF252525" BorderBrush="#FF3A3A3A" BorderThickness="1" CornerRadius="6"
                                            Margin="30,15,0,0" Padding="10"
                                            IsEnabled="{Binding Path=IsChecked, ElementName=rbUseProfile}">
                                        <ListBox ItemsSource="{Binding AvailableProfiles}" SelectedItem="{Binding SelectedProfile}"
                                                 Style="{StaticResource ModernListBox}" Height="120" BorderThickness="0" Background="Transparent">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#FF2A2A2A" BorderBrush="#FF404040" BorderThickness="1" CornerRadius="4" Padding="10" Margin="2">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13" Foreground="#FF00E676"/>
                                                                <TextBlock FontSize="11" Foreground="#FF888888" Margin="0,2,0,0">
                                                                    <Run Text="{Binding ClassName}"/>
                                                                    <Run Text="."/>
                                                                    <Run Text="{Binding MethodName}"/>
                                                                </TextBlock>
                                                            </StackPanel>

                                                            <TextBlock Grid.Column="1" Text="✓" FontSize="16" Foreground="#FF00E676" VerticalAlignment="Center"
                                                                       Visibility="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <!-- Modern Footer -->
            <Border Grid.Row="2" Background="#FF1E1E1E" CornerRadius="0,0,12,12">
                <Grid Margin="25,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="💡 Tip: Profiles save your injection settings for quick reuse"
                               Foreground="#FF666666" FontSize="11" VerticalAlignment="Center" TextWrapping="Wrap"/>

                    <Button Grid.Column="1" Content="Cancel" Style="{StaticResource SecondaryButton}" Width="100" Margin="0,0,15,0"
                            IsCancel="True" Click="Cancel_Click" Height="35"/>
                    <Button Grid.Column="2" Content="💾 Save Profile" Style="{StaticResource ModernButton}" Width="130"
                            IsDefault="True" Command="{Binding SelectCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Height="35" FontSize="13"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>